-- All questions use a sample table Employees:

-- Employees
-- | EmpID | Name | Department | Salary | City | Age | Bonus |
CREATE TABLE Employees(
EmpID int primary key,
Name varchar(20),
Department varchar(20),
Salary INT default 30000,
City varchar(20),
Age INT NOT NULL,
Bonus INt default 2000
);

INSERT INTO Employees (EmpID, Name, Department, Salary, City, Age, Bonus) values 
(101,'A','IT',35000,'HYD',45,3000),
(102,'B','IT',59000,'DELHI',50,3000),
(103,'C','DATA',45000,'HYD',25,3000),
(104,'D','intern',30000,'HYD',45,null),
(105,'E','DATA',45000,'KASHMIR',36,3000),
(106,'F','HR',50000,'BANGALORE',43,null);

-- 1.Write a query to display all columns from the Employees table.
SELECT * FROM Employees;

-- 2.Display the unique department names from the Employees table.
-- SELECT DISTINCT department
-- FROM Employees;

-- 3.Display employees who work in the 'IT' department.
-- SELECT NAME,department
-- FROM employees
-- WHERE department='IT';

-- 4.Display all employees ordered by Salary in descending order.
-- SELECT NAME,Salary
-- FROM employees
-- ORDER BY Salary DESC;

-- 5.Display employees from the IT department AND earning salary greater than 50,000.
-- SELECT *
-- FROM employees
-- WHERE department='IT' AND salary>50000;

-- 6.Display employees who are either from 'HR' department OR from 'Finance'.
-- SELECT *
-- FROM employees
-- WHERE department='Finance' OR department='HR';

-- 7.Display employees not living in 'Delhi'.
-- SELECT *
-- FROM employees
-- WHERE NOT CITY = 'DELHI';

-- 8.Insert a new employee record into the Employees table.
-- INSERT INTO Employees values
-- ('7','G','FINANCE',null,'BIHAR','21',default);
-- SELECT*from Employees;

-- 9.Display employees whose Bonus is NULL.
-- select *
-- from employees
-- where Bonus is null;

-- 10.Update the salary of employee with EmpID = 101 to 70,000.
-- update Employees
-- set salary = 70000
-- where empid=101;

-- 11.Delete all employees who belong to the 'Intern' department.
-- Delete
-- from employees
-- where department='intern';
-- select*from employees;

-- 12.Display the top 5 highest-paid employees.
-- select *
-- from employees
-- order by salary DESC
-- FETCH first 5 ROWS ONLY;

-- 13.Find the total salary paid to all employees.
-- select sum(salary) as "total salary"
-- from employees;

-- 14.Find the minimum and maximum salary in the Employees table.
-- select min(salary) as "min salary",
--       max(salary) as "max salary"
-- from Employees;

-- 15.Count how many employees are working in the IT department.
-- select count(*)
-- from employees
-- where department='IT';








